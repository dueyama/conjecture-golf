# Conjecture Golf Canonical Arena Branch

Branch: `arena/season-2`

This snapshot contains only accepted public game commands.
Replay it with:

```bash
python -m conjecture_golf.replay transcript.jsonl --season-scoring
```

## Latest AI Arena Packet

`AI_ARENA_PACKET.latest.json` is the latest machine-readable state for
constrained AI players. Refresh it after every `/cg` verdict before
choosing a move.
